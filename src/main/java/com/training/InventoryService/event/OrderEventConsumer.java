package com.training.InventoryService.event;

import com.training.InventoryService.service.InventoryService;
//import com.training.OrderService.event.OrderEvent;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Component;
/*

@Component
public class OrderEventConsumer {
*/
/*
    @Autowired
    private InventoryService inventoryService;

    @KafkaListener(topics = "order-topic", groupId = "zion-group")
    public void consumeOrderEvent(OrderEvent event) {
        System.out.println("Received Order Event: " + event);
        inventoryService.updateStock(event.getOrderId());
    }*//*

}*/
